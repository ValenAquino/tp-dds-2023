{{#partial "principal"}}
    <div class="container">
        <div class="row">
            <div class="col-sm-9 col-md-7 col-lg-5 mx-auto">
                <div class="rounded-3 my-5 p-4 p-sm-5">
                    <h2 class="text-center">Reportar Incidente</h2>
                    <hr>
                    <form id="incident-form" method="POST" >
                        <div class="form-group">
                            <div class="row">
                                <label for="servicios">Servicios</label>
                                <select class="form-select" id="servicios" name="servicio"> <!-- Agregamos name="servicio" para identificar el campo en el servidor -->
                                    {{#each servicios}}
                                        <option value="{{getId}}">{{getDescripcion}}</option>
                                    {{/each}}
                                </select>
                            </div>
                        </div>
                        <div class="form-group">
                            <label for="observaciones">Observaciones</label>
                            <textarea class="form-control" id="observaciones" name="observaciones" rows="3" placeholder="Agregar observaciones"></textarea> <!-- Agregamos name="observaciones" para identificar el campo en el servidor -->
                        </div>
                        <div class="text-center">
                            <button type="submit" class="btn btn-primary">Reportar Incidente</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
    <script>
    document.addEventListener('DOMContentLoaded', function () {
        const establecimientoDropdown = document.getElementById('establecimiento');
        const serviciosDropdown = document.getElementById('servicios');

        establecimientoDropdown.addEventListener('change', function () {
            const selectedEstablecimiento = establecimientoDropdown.value;
            //Agregar logica
        });
    });
    </script>
{{/partial}}

{{> layouts/logged.html.hbs }}
